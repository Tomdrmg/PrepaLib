{% extends 'base.html.twig' %}

{% block body %}
    <twig:Loader />
    <twig:Header user="{{ app.user }}" />
    <main id="main-{% block mainid %}empty{% endblock %}" class="min-h-[100dvh] flex flex-col">
        {% block main %}
        {% endblock %}
    </main>
    <twig:Footer user="{{ app.user }}" />
{% endblock %}
