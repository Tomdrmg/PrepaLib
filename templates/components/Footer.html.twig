<footer class="relative shadow-footer bg-background-700/60 py-8 z-10">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
            <div>
                <h3 class="text-lg font-semibold mb-4">PrépaLib</h3>
                <p class="text-text-300">
                    {% if admin %}
                        Gérer et visualiser toutes les données de la plateforme dans cette espace administrateur.
                    {% else %}
                        Préparez-vous aux concours pour les grandes écoles en utilisant les ressources communautaires.
                    {% endif %}
                </p>
            </div>
            {% if admin %}
                <div>
                    <h4 class="text-md font-semibold mb-4">Données</h4>
                    <ul class="space-y-2">
                        <li><a href="{{ path('app_admin_users') }}" class="text-text-300 hover:text-text-100 transition">Utilisateurs</a></li>
                        <li><a href="{{ path('app_admin_subjects') }}" class="text-text-300 hover:text-text-100 transition">Matières</a></li>
                        <li><a href="{{ path('app_admin_tags') }}" class="text-text-300 hover:text-text-100 transition">Tags</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-md font-semibold mb-4">Administrateur</h4>
                    <ul class="space-y-2">
                        <li><a href="{{ path('app_profile') }}" class="text-text-300 hover:text-text-100 transition">Mes Informations</a></li>
                        <li><a href="{{ path('app_security') }}" class="text-text-300 hover:text-text-100 transition">Sécurité</a></li>
                        <li><a href="{{ path('app_home') }}" class="text-text-300 hover:text-text-100 transition">Retour au site</a></li>
                        <li><a href="{{ path('app_logout') }}" class="text-text-300 hover:text-text-100 transition">Déconnexion</a></li>
                    </ul>
                </div>
            {% else %}
                <div>
                    <h4 class="text-md font-semibold mb-4">Ressources</h4>
                    {% if app.user %}
                        <ul class="space-y-2">
                            {% for subject in subjects %}
                                <li><a href="{{ path('app_subject', {'subject': subject.id}) }}" class="text-text-300 hover:text-text-100 transition">{{ subject.name }}</a></li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <p class="text-text-300 md:me-20">Vous devez être connecté pour accéder aux ressources.</p>
                    {% endif %}
                </div>
                <div>
                    <h4 class="text-md font-semibold mb-4">{% if app.user %}Mon Apprentissage{% else %}Rejoindre{% endif %}</h4>
                    <ul class="space-y-2">
                        {% if app.user %}
                            <li><a href="{{ path('app_profile') }}" class="text-text-300 hover:text-text-100 transition">Mes Informations</a></li>
                            <li><a href="{{ path('app_security') }}" class="text-text-300 hover:text-text-100 transition">Sécurité</a></li>
                            <li><a href="{{ path('app_logout') }}" class="text-text-300 hover:text-text-100 transition">Déconnexion</a></li>
                        {% else %}
                            <li><a href="{{ path('app_login') }}" class="text-text-300 hover:text-text-100 transition">Connexion</a></li>
                            <li><a href="{{ path('app_register') }}" class="text-text-300 hover:text-text-100 transition">Inscription</a></li>
                        {% endif %}
                    </ul>
                </div>
                <div>
                    <h4 class="text-md font-semibold mb-4">Contribuer</h4>
                    {% if app.user %}
                        <ul class="space-y-2">
                            <li><a href="{{ path('app_contribute_suggestion') }}" class="text-text-300 hover:text-text-100 transition">Suggestion</a></li>
                            <li><a href="{{ path('app_contribute_chapter') }}" class="text-text-300 hover:text-text-100 transition">Créer un Chapitre</a></li>
                            <li><a href="{{ path('app_contribute_exercise') }}" class="text-text-300 hover:text-text-100 transition">Créer un Exercice</a></li>
                        </ul>
                    {% else %}
                        <p class="text-text-300 md:me-20">Vous devez être connecté pour accéder aux ressources.</p>
                    {% endif %}
                </div>
            {% endif %}
        </div>
        <div class="border-t border-foreground-300 mt-8 pt-6 text-center text-text-300">
            <p>&copy; {{ "now"|date("Y") }} PrépaLib. Tous droits réservés.</p>
        </div>
    </div>
</footer>
